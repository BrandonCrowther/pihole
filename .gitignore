# Local environment / secrets — never commit
.env
.env.*

# Pi-hole runtime state: databases, leases, TLS keys, gravity blocklists.
# All regenerated by the container on boot; not part of the portable config.
etc-pihole/
var-log-pihole/
dnsmasq.d/

# Editor / OS cruft
*.swp
*.swo
*~
.DS_Store
